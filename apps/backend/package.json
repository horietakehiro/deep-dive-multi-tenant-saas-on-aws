{
  "name": "@intersection/backend",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest",
    "ampx": "npx ampx",
    "build": "npx tsc && npx vite build",
    "check": "npx tsc"
  },
  "dependencies": {
    "@aws-sdk/client-cognito-identity": "^3.879.0",
    "@aws-sdk/client-cognito-identity-provider": "^3.879.0",
    "@aws-sdk/client-secrets-manager": "^3.879.0",
    "aws-amplify": "^6.15.5"
  }
}
